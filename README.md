Contains my common configurations and a script to automatically set them up.
It does so by appending .bak to the original config file and symlinking to
the appropriate file in this repo.
```shell
./use_config --help
```
